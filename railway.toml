[build]
builder = "nixpacks"

[deploy]
startCommand = "python manage.py migrate && python manage.py seed_workouts && gunicorn nutri_backend.wsgi --bind 0.0.0.0:$PORT --workers 2 --timeout 60"
healthcheckPath = "/"
healthcheckTimeout = 30
restartPolicyType = "on_failure"